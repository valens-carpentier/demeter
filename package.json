{
  "name": "farmfactory",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run migrations/1_deploy_contracts.js --network baseSepolia",
    "deploy:local": "hardhat run migrations/1_deploy_contracts.js --network hardhat"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.4.1",
    "hardhat": "^2.22.18"
  },
  "dependencies": {
    "@openzeppelin/contracts-upgradeable": "^5.2.0",
    "date-fns": "^4.1.0"
  }
}
